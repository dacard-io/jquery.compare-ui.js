/**
** jquery.compare-ui.js - Version 1.0
**/

(function($){
	$.fn.compareUI = function(options) {

		// Setup default options
		var defaults = {
			compareButton: '.compareButton'
		}

		var options = $.extend(defaults, options); // Extend the defaults as well as any user-defined options

	}
});